spring:
  application:
    name: server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/hstha/app-configuration

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8080