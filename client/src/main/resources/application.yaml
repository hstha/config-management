spring:
  application:
    name: client

  profiles:
    active: dev

  config:
    import: optional:configserver:http://localhost:8080

server:
  port: 8000

management:
  endpoints:
    web:
      exposure:
        include: refresh
